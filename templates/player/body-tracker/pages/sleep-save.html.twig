{% extends 'player/base-player.html.twig' %}

{% block page_content %}
    {{ form_start(form) }}
        <div>
            In bed at
            {{ form_widget(form.inBed) }}
        </div>
        <div>
            Out of bed at
            {{ form_widget(form.outOfBed) }}
        </div>
        <div>
            <a href="{{ path('page_sleeps_for_current_user') }}" class="button cancel-color">Cancel</a>
            <button type="submit" class="button action-color">Save</button>
        </div>
    {{ form_end(form) }}
{% endblock %}