{% extends 'admin/base-admin.html.twig' %}

{% block page_content %}
    {{ form_start(form) }}
    <div>
        Name
        {{ form_widget(form.name) }}
    </div>
    <div>
        <a href="{{ path('page_admin_movements') }}" class="button cancel-color">Cancel</a>
        <button type="submit" class="button action-color">Save</button>
    </div>
    {{ form_end(form) }}
{% endblock %}