{% extends 'admin/base-admin.html.twig' %}

{% block page_content %}
    {{ form_start(form) }}
    <div>
        Title
        {{ form_widget(form.title) }}
    </div>
    <div>
        <a href="{{ path('page_admin_routines') }}" class="button cancel-color">Cancel</a>
        <button type="submit" class="button action-color">Save</button>
    </div>
    {{ form_end(form) }}
{% endblock %}